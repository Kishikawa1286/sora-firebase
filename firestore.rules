rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }

    match /slack_tokens_v1/{token} {
      allow read, write: if false;
    }
  }
}
